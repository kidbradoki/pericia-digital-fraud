import os

# Define o conteúdo do arquivo de requisitos
# Flask para o app e Gunicorn para o servidor do Render
content = """flask
gunicorn
"""

# Caminho onde o arquivo será salvo (raiz do seu projeto)
file_path = "requirements.txt"

try:
    with open(file_path, "w") as f:
        f.write(content)
    print(f"Sucesso! Arquivo '{file_path}' criado com as dependências.")
    print(f"Localização real: {os.path.abspath(file_path)}")
except Exception as e:
    print(f"Erro ao criar o arquivo: {e}")
